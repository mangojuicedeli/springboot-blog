server:
  port: 8009

logging:
  level:
    org.springframework: INFO

spring:
  messages:
    basename: messages
  security:
    user:
      name: username
      password: passw0rd
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: "*"